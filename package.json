{
  "name": "ultimate-chess",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Ultimate Chess — Telegram Mini App (static frontend + serverless backend)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node tests/serve.js",
    "test": "node tests/run.js && node tests/auth.js"
  }
}
